#include <stdio.h>

int main(int argc, char const *argv[])
{

    int tc, ans[1000], i, temp, temp[1000], num;

    scanf("%d", &tc);
    while (tc--) {
        temp = 0;
        scanf("%d", &num);

        while (1) {


        }
    }

    return 0;
}